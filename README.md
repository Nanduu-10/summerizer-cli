# summerizer-cli

A simple command-line tool to summarize text files using Hugging Face Transformers.

## 🔧 Features
- Summarizes long text files into concise summaries
- Uses `t5-small` model from Hugging Face
- Supports chunking and consolidation for large inputs
- Saves output to `summary.txt`

## 🚀 Installation

```bash
pip install transformers sentencepiece
